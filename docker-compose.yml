services:
  planemanagement:
    build: .
    image: planemanagement:local
    restart: unless-stopped
    env_file: .env
    environment:
      - PILOTS_JSON=/data/pilots.json
    volumes:
      - ./data:/data
