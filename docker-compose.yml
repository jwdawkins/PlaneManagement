services:
  planemanagement:
    build: .
    image: planemanagement:local
    restart: unless-stopped
    env_file: .env
    environment:
      - PILOTS_JSON=/data/pilots.json
      - FLYSTO_OUTPUT=/data/flight_logs.json
    volumes:
      - ./data:/data
